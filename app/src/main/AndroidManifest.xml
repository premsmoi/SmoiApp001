<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smoiapp001">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_wallet"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_wallet_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.MainActivity"
            android:configChanges="orientation"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.ManageTransactionActivity"
            android:configChanges="orientation"
            android:screenOrientation="portrait"
            android:label="@string/add_transaction_activity_name" />

        <receiver android:name=".AlarmNotificationReceiver" android:enabled="true" >
            <intent-filter>
                <action android:name="com.example.smoiapp001.NOTIFICATION_ACTION" />
            </intent-filter>
        </receiver>

    </application>

</manifest>